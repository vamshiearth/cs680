package umbcs680.hw09;

public interface ColorAdjuster {

    /**
     * Interface method for adjusting the color.
     *
     * @param color the color to be adjusted
     * @return a new Color object with the adjusted values
     */
    Color adjust(Color color);  // Method to adjust the color and return a new Color
}
